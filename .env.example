# Supabase (free tier). These are public client keys — access is protected by
# Row Level Security. The app also ships with working defaults baked in, so
# setting these is optional for local dev.
VITE_SUPABASE_URL=https://daacilgagkphafpjdcte.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_pfQ_kyf47PV-p_E2fDG3wQ__5MaWnsZ

# ── SlipOK Verification (free tier available) ─────────────────────────────────
# Sign up at https://slipok.com/ → create a branch → get your Branch ID + API Key.
# The free tier covers development / low-volume usage.
# Set these as Supabase secrets (NOT in the client):
#   supabase secrets set SLIPOK_BRANCH_ID=xxx SLIPOK_API_KEY=yyy
#
# SLIPOK_BRANCH_ID=your_slipok_branch_id
# SLIPOK_API_KEY=your_slipok_api_key
